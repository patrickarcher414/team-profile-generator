// Import data from Employee.js
const Employee = require("../lib/Employee");
// Instantiate new Employee Object
const Employee = new Employee();

// test employee constructor
test("creates a new Employee object", (req, res) => {
  expect(res).toBe();
});

// test getName function
test("returns the name of employee", (req, res) => {
  expect(res).toBe();
});

// test getId function
test("returns the id of employee", (req, res) => {
  expect(res).toBe();
});

// test getEmail
test("returns the email of employee", (req, res) => {
  expect(res).toBe();
});

// test getRole
test("returns the role of employee", (req, res) => {
  expect(res).toBe();
});
